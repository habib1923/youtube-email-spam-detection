# 📧🎥 YouTube & Email Spam Detection using PySpark + Gradio

This project detects spam in both **emails** and **YouTube comments** using machine learning models built with **PySpark**.

## 🚀 Features
- Train multiple models (Naive Bayes, Random Forest, Logistic Regression)
- Evaluate performance (Accuracy, F1-score)
- Interactive Gradio app for real-time predictions

## ⚙️ How to Run
```bash
pip install -r requirements.txt
